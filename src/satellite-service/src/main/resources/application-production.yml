
server:
    port: ${SERVER_PORT:3000}
manager:
  api:
    url: http://localhost:8081

firebase:
  serviceAccountPath: src/main/resources/serviceAccountKey.json
  databaseUrl: https://ai-pracns-default-rtdb.asia-southeast1.firebasedatabase.app

buffer:
  maxSize: 500

state:
  report:
    interval:
      ms: 5000
