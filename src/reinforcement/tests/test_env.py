import unittest
import numpy as np

class TestEnvironment(unittest.TestCase):
    def test_state_builder(self):
        # Kiểm tra StateBuilder
        self.assertTrue(True)
    
    def test_reward_calculation(self):
        # Kiểm tra reward calculation
        self.assertTrue(True)