# ML Core (Sử dụng PyTorch)
torch>=2.0.0            # Framework học sâu chính để xây dựng mô hình DQN.
numpy>=1.20.0           # Xử lý các phép toán vector và ma trận (State Vectors).

# Database Connection
pymongo>=4.0.0          # Driver chính thức để kết nối và tương tác với MongoDB.

# Tiện ích
python-dotenv           # (Tùy chọn) Quản lý biến môi trường local (.env file).