# Cloud MongoDB Connection URI
# Replace this with your actual MongoDB Atlas connection string
CLOUD_MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Optional: Sync interval in seconds (default: 15)
# SYNC_INTERVAL_SECONDS=15

# Optional: Database name (default: sagsin_network)
# DB_NAME=sagsin_network